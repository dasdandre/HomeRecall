@page "/settings"
@rendermode InteractiveServer

<MudText Typo="Typo.h4">Settings</MudText>
<MudText Class="mt-4">Configuration options will appear here.</MudText>
<MudText>Current Backup Path: @(Environment.GetEnvironmentVariable("backup_path") ?? Path.Combine(Directory.GetCurrentDirectory(), "backups"))</MudText>
